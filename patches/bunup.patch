--- a/dist/plugins.js
+++ b/dist/plugins.js
@@ -145,7 +145,7 @@ function isDirectoryPath(filePath) {
   }
 }
 async function copyDirectory(sourcePath, finalDestinationPath) {
-  await Bun.$`cp -r ${sourcePath} ${finalDestinationPath}`;
+  await fs.promises.cp(sourcePath, finalDestinationPath, {recursive: true});
 }
 export {
   unused,